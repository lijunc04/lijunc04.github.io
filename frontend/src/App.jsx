import { useState } from 'react'
import React from 'react'
import './App.scss'

function App() {
  const [count, setCount] = useState(0);

  return (
    <BrowserRouter>
    </BrowserRouter>
  )
}

export default App
